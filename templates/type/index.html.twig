{% extends 'backbase.html.twig' %}


{% block title %}
	Liste des types
{% endblock %}


{% block body %}



	 <div class="row ">
            <div class="col-12 grid-margin">
              <div class="card">
                <div class="card-body">

               <h4 class="card-title">Liste des types <a href=" {{ path ('add_type') }} " class="btn btn-success btn-fw float-right">Ajouter Type</a></h4>


                  <div class="table-responsive">
                    <table class="table">
                      <thead>
                        <tr>
                          <th> Type </th>
                         
                        </tr>
                      </thead>

                      {% for data in list %}
                        <tr>
                         
                          <td> {{ data.type }} </td>
                         
                       


                      
                         <td>
  <a href="{{ path('delete_type', {'id': data.idtype}) }}" class="btn btn-outline-danger btn-fw;">Supprimer</a>
</td>


                          <td>
                             <a href="{{ path('update_type', {'id': data.idtype}) }}"  class="btn btn-outline-warning btn-fw;"> Modifier</a>
                          </td>
                        </tr>
                          
                         {% endfor %}
                       
                         
                

{% endblock %}
